# MXH Route 1.14.0-mxh.14

- 手动改选受监测的入口节点后，该节点成为当前首选；故障时仍按配置顺序切换到健康备用，稳定恢复后切回手动首选。
- 故障切换明确排除当前节点，不会把故障节点再次选为自己的备用；全部备用不可用时保持当前选择，不转为直连。
- 改选恰好发生在探测途中时，丢弃旧一轮结果，保存新的首选并记录事件；旧版“手动选择后暂停”的运行快照会迁移。
- 系统代理健康守护仅在当前节点确实受入口监测时让故障切换优先处理；未纳入自定义顺序的节点会在界面提示，并允许整体代理恢复机制接管。
- 设置页区分“配置首选”和“当前首选”。本次不更改权威代理配置、节点顺序或服务器。

Windows x64 签名安装包；可通过应用内“检查更新”升级。安装更新前，当前运行的旧版仍沿用旧故障切换行为。
