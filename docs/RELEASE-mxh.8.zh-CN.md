# MXH Route 1.14.0-mxh.8

- 将 mxh.7 的配置驱动策略接入设置页“自动故障切换”面板。
- 支持功能开关、分组选择、跟随组内顺序／自定义顺序、拖拽和上下按钮排序、候选增删。
- 高级参数默认折叠，时间以秒显示；前后端共同校验范围。
- 显示当前运行节点、监测／暂停状态及本次运行最近自动切换。
- 保存只原子更新同一份 `priority-failover.json`；重载需单独确认。配置被外部修改或当前配置已切换时，拒绝过期保存／重载。
- 保留配置文件编辑入口和 mxh.6 的滚动日志。未修改节点凭据、权威配置或运行中的代理。

已完成组件隔离预览的布局与交互检查，包括节点排序、分组切换、保存不重载、重载确认和非法输入拦截；预览使用虚构节点与模拟后端，不是对真实节点进行故障注入。

安装后进入“设置”即可找到面板。仍需保持应用运行或留在托盘，自动切换不保证已有连接无缝迁移。
